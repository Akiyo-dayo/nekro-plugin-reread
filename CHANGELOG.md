# 更新日志

## 1.0.0（2026-08-20）

NekroAgent「复读姬」插件首个公开发布。

- 文本 / 图片 / QQ 表情（含表情包）分开设复读阈值，先掷打断概率，再掷复读概率。
- 默认要求不同人接力；支持屏蔽词、屏蔽 QQ、群白/黑名单、冷却、过长文本、指令和 @ 机器人过滤。
- 跟读或打断时默认拦住 AI：消息记入历史，但不叫醒 Agent。
- 指令：`复读姬`、`复读榜`、`复读姬帮助`；超管可开关本群或清空连击。
- AI 可调用「查看复读榜」。
- 仅支持 `onebot_v11` 群聊。安装时把仓库内 `reread` 目录放到 `plugins/workdir/reread`。
- 核心判定不依赖 NekroAgent，可单独跑 `python -m pytest tests -q`。
